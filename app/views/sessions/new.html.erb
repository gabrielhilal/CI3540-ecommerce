<h1>Login</h1>
<% #here we are using the form_tag instead of form_for, because this form is not related to a model (database) %>
<% #the params will be passed to the CREATE action in the sessions controller when the form is submitted %>
<%= form_tag sessions_path do %>
  <div class="field">
    <%= label_tag :email %><br />
    <%= text_field_tag :email, params[:email] %>
  </div>
  <div class="field">
    <%= label_tag :password %><br />
    <%= password_field_tag :password %>
  </div>
  <div class="actions">
    <%= submit_tag "Login" %>
  </div>
<% end %>
<br />
<div>New customer? <%= link_to "Sign in", new_user_path %></div>
